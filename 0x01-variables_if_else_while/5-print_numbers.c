#include <stdio.h>
/**
 * main - Entry
 * Description: count from 0 to 10
 * Return: always 0
 */
int main(void)
{
	int num;

	for (num = 0; num <= 10; num++)
	{
		printf("%d", num);
	}
	return (0);
}
